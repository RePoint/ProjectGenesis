<h3 class="ms-accentText" id="headScriptTitle">JavaScript</h3>
<div class="sectionDescription">
    JavaScript references can be regular links (http://site.com/Style Library/style.js) or relative links (/Style Library/style.js)
</div>

<form id="updateScriptLinksForm" method="get" action="">
    
    <table id="formTemplates" class="table table-plain table-icon-primary" style="margin-top: 10px;float:left">
 
        <thead>
            <tr>
                <th>Title</th>
                <th>Script Source Url</th>
                <th>Sequence</th>
                <th></th>
            </tr>
        </thead>
        <tbody>

            {{#each item in ScriptLinks }}
            <tr>
                {{#if item.IsEditing}}
                    <td>{{input id="scriptLinkTitle" name="scriptLinkTitle" value=item.Title class="form-control" required=true }}</td>
                    <td>{{input id="scriptLinkSrc" name="scriptLinkSrc" value=item.ScriptSrc class="form-control" required=true  }}</td>
                    <td>{{input id="scriptLinkSeq" name="scriptLinkSeq" value=item.Sequence class="form-control" digits=true maxlength=3 }}</td>
                {{else}}                  
                    <td>{{item.Title}}</td>
                    <td>{{item.ScriptSrc}}</td>
                    <td>{{item.Sequence}}</td>
                {{/if}}

                <td width="80px" class="pointer">
                    {{#if item.IsEditing}}
                    <a {{ action 'updateRow' item }} href="#" title="Update" class="glyphicon glyphicon-save"><span class="screenReader">Update</span></a>
                    {{else}}
                    <a {{ action 'editRow' item }} href="#" title="Edit" class="glyphicon glyphicon-edit"><span class="screenReader">Edit</span></a>
                    <a {{ action 'deleteRow' item }} href="#" title="Delete" class="glyphicon glyphicon-remove-circle"><span class="screenReader">Delete</span></a>
                    {{/if}}
                </td>
            </tr>
            {{/each}}
        </tbody>
        <tfoot>
            <tr>             
                <td colspan="4" style="text-align:center">
                    {{#if rowNotInEditMode}}
                        <button {{ action 'addRow' }} type="button"  class="btn btn-default btn-sm">
                            <span class="glyphicon glyphicon-ok" aria-hidden="true"></span> Add
                        </button>
                    {{else}}
                        <button {{ action 'addRow' }} type="button" disabled="true" class="btn btn-default btn-sm">
                            <span class="glyphicon glyphicon-ok" aria-hidden="true"></span> Add
                        </button>
                    {{/if}}
                </td>               
            </tr>
        </tfoot>
    </table>
    <div class="col-md-12" style="clear:left">
        <div id="scriptError" class="alert-danger alert" data-alert="alert" role="alert">
            <p>Oh no. An error has occurred. Please give the following information to your administrator so they can fix the issue...</p>
            <div id="scriptErrorDetails" class="errorDetails"></div>
        </div>
        <div id="scriptSuccess" class="alert-info alert" data-alert="alert" role="alert">
            <p><strong>Success:</strong> Your changes have been made.</p>
        </div>
    </div>  
</form>

